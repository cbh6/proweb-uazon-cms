export class User {
  title: number;
  name: string;
  email: string;
  role: string;
  address: string;
  cp: number;

  constructor() {
    // forma rapida, asigname en this, los atributos de obj
    // Object.assign(this, obj);
  }
}
